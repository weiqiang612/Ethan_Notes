- [x] 给 `IntentType` 加 `highRisk` 属性，消灭外部列表
- [x] 补上 `REORDER` 意图，修复死功能
- [x] IntentRecognitionAdvisor` 改为调用 `CustomerIntentRecognitionService , 修复归一化逻辑绕过的安全漏洞
- [x] 修复 `OTHER` 意图不写记忆的问题
- [x] IntentType 加 category / domain，之后路由可基于 category / domain 自动推导
- [x] 路由和 RAG 开关改为基于 `category` 属性
- [x] 工具权限改为 domain + intent 两级构建