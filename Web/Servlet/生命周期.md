# Servlet生命周期

1. 加载与实例化：容器通过反射机制创建Servlet实例
2. 初始化：执行init方法，通常用于读取配置文件、初始化数据库连接等
3. 请求处理：每当有一个请求指向该Servlet，容器就启动一个新线程，调用service方法，在HttpServlet中，service()会根据HTTP请求的方法，自动调用doget/dopost
4. 销毁：当web应用被卸载或服务器被关闭时，释放资源，如关闭数据库连接、停止后台线程、将内存数据持久化。执行完 destroy() 后，垃圾回收器（GC）会回收该对象