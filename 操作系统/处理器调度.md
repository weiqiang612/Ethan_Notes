# 处理器调度

>
>
>1. 处理器调度分为哪三级，它们的主要任务是什么
>
>     * 作业调度(高级调度)：根据某种策略或算法，从后备队列选择若干作业，分配必要资源，加载程序和数据并等待进程调度为其指派处理器
>     * 内存调度(中级调度)：动态的将一些进程部分或全部从内存交换到外存或反之，由此决定哪些进程参与CPU竞争
>     * 进程调度(低级调度)：按某种策略或算法选取某个(或某几个)就绪进程占用处理器并实现进程间转换
>2. 处理器调度算法中，平均周转时间最短的是哪个，平均带权周转时间最短的是哪个：短作业优先